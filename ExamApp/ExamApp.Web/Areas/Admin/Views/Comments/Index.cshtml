@model IEnumerable<ExamApp.Models.Comment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@(Html
    .Kendo().Grid(Model)
    .Name("comments-grid")
    .Columns(columns =>
            {
                columns.Bound(x => x.Content);
                //columns.Bound(x => x.Author.UserName);
                //columns.Bound(x => x.Author.Email);
                //columns.Bound(x => x.CreatedOn);
                //columns.Bound(x => x.Book.Title);
                //columns.Bound(x => x.Book.Category.Name);
                columns.Template(@<text>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id })
                </text>);
                columns.Template(@<text>
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </text>);
            })
    .Sortable(sort =>
            {
                sort.AllowUnsort(false);
                sort.SortMode(GridSortMode.SingleColumn);
            })
    .Pageable()
    .Filterable()
    .DataSource(dataSource => dataSource
        .Server()
        .Read(read => read.Action("Index", "Comments"))
        .PageSize(3)))

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Author.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ticket.AuthorId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Content)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Author.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ticket.AuthorId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Content)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>*@
