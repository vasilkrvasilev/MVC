@model IEnumerable<Twitter.Models.Tag>

<br />

@if (Model.Count() > 0)
{
    foreach (var item in Model)
    {
        <strong> @Html.ActionLink(item.Name, "ByTag", "Home", new { selectedTag = item.Id }, null) </strong>
    }
}
else
{
    <strong>No Tags found</strong>
}

<br />
